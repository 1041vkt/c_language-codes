// 9. Write a c program to find maximum between three number.

//    Using of Nested if-----------

    int n1,n2,n3;
    printf("Enter the three number :");
    scanf("%d%d%d",&n1,&n2,&n3);

    if(n1>n2)
    {
        if(n1>n3)
        {
            printf("n1 is biggest");
        }
        else
        {
            printf("n3 is biggest");
        }
    }
    else
    {
        if(n2>n3)
        {
            printf("n2 is biggest");
        }
        else
        {
            printf("n3 is biggest");
        }
    }
}


// 9. Write a c program to find maximum between three number.

#include "stdio.h"
void main()
{
    /*   Using of ladder if-----------


    int x,y,z;
    printf("Enter the three number :");
    scanf("\n%d\n%d\n%d",&x,&y,&z);

    if(x>y && x>z)
    {
        printf("x is bigger ");
    }
    else if(y>x && y>z)
    {
        printf("y is bigger");
    }
    else
    {
        printf("z is bigger");
    }
    */
